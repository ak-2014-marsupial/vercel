import React from 'react';
import {RegisterComponent} from "../features/auth/Register/Register.component";

const RegisterPage = () => {
        return (
    <RegisterComponent/>
    );
};

export {RegisterPage};